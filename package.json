{
  "name": "devcon1solutions-api",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "server":"cd functions/ && npm run server",
    "deploy":"cd functions/ && npm run deploy",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "none",
  "dependencies": {
    "firebase": "^10.6.0"
  }
}
